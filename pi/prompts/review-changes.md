---
description: Run an independent read-only review of current changes
argument-hint: "[focus]"
---
Use the `subagent` tool with the `reviewer` agent to review the current working tree changes.

Focus, if provided:
$ARGUMENTS

Then synthesize the reviewer verdict in the main session and decide next steps. Do not let the reviewer modify files.
