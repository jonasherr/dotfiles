---
description: Run one or more read-only scout subagents and synthesize a compact answer
argument-hint: "<task>"
---
Use the `subagent` tool with the `scout` agent for this task:

$ARGUMENTS

If the task has clearly independent areas, split it into parallel scout tasks. Otherwise use a single scout. Synthesize the result in the main session; do not paste unnecessary raw scout output.
