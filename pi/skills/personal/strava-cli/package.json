{
  "name": "strava-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "strava-local": "./dist/strava-local.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "check:dist": "node scripts/check-dist-current.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@10.11.0"
}
