# Claude Code Configuration

This file is symlinked to `~/.claude/CLAUDE.md`.

Shared skills are managed from `pi/skills/` and exposed through `~/.agents/skills/`.
Run `./pi/install-skills.sh` from the dotfiles repo after adding or moving a public skill.
